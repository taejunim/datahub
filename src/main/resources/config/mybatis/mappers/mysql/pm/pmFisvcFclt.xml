<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="net.pm.mapper.PmFisvcFcltMapper">
    <insert id="insertPmFisvcFclt" parameterType="java.util.List">
        INSERT IGNORE INTO pm_fisvc_fclt (
             ID
           , LOCATION_X
           , LOCATION_Y
        ) VALUES
        <foreach collection="list" item="item" separator=",">
          (
            #{item.id}
          , #{item.location_x}
          , #{item.location_y}
          )
        </foreach>
    </insert>

    <select id="selectPmFisvcFcltList" resultType="gInfra">
        SELECT
            a.ID AS id
             , a.LOCATION_X AS location_x
             , a.LOCATION_Y AS location_y
        FROM pm_fisvc_fclt AS a
    </select>
</mapper>